/**
 * Bump APP_VERSION (and usually package.json version) on every user-facing release.
 * Splash is shown once per version until the user dismisses or updates.
 */
export const APP_VERSION = '1.7.5';

export const UPDATE_SPLASH = {
  title: 'Polish pass + deeper remedies',
  message: 'Policies updated for Maker Studio; remedies library expanded and deepened.',
  highlights: [
    '400+ remedy monographs with fuller free content',
    'Policies & FAQ cover Maker Studio and product vs service ID',
    'Cleanup and production polish',
  ],
};

export const SEEN_VERSION_KEY = 'ha_seen_app_version';
